# Z.ai Agent Entry Point

Treat this repository as the only source of truth. Read `AGENTS.md`, then the canonical architecture, requirements, dependency graph, program state, and implementation guide it references.

WorkflowOS is an external execution authority and must never be reimplemented inside Floooz. Select only dependency-eligible FZ work items and produce objective acceptance evidence before completion.