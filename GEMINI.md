# Android Studio Gemini Entry Point

Treat this repository as the only source of truth. Read `AGENTS.md` first, then `spec/development-state/program-state.json`, `spec/architecture-lock.md`, `spec/architecture.md`, `spec/requirements.md`, `spec/work-items.md`, `spec/dependency-graph.md`, and `docs/android/ANDROID-IMPLEMENTATION.md`.

Android is a device runtime, not the authoritative backend. Follow the same work-item, dependency, policy, privacy, testing, and authority rules as every other implementation agent. Never infer missing requirements from conversation history.